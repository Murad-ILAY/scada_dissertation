spring:
  application:
    name: scada
  datasource:
    url: jdbc:mysql://localhost:3306/scada
    username: root
    password: Tyutru64!13579
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
